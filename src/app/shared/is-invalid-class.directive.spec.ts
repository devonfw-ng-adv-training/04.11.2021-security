import { IsInvalidClassDirective } from './is-invalid-class.directive';

describe('IsInvalidClassDirective', () => {
  it('should create an instance', () => {
    const directive = new IsInvalidClassDirective();
    expect(directive).toBeTruthy();
  });
});
